import type { ReactElement } from "react";

export const Header = (): ReactElement => {
  return (
    <header className="header">
      <h1 className="header-h1">Account Registration</h1>
    </header>
  );
};
